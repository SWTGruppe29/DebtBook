<Window x:Class="DebtBook.Views.DebtorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DebtBook.Views"
        xmlns:viewModels="clr-namespace:DebtBook.ViewModels"
        mc:Ignorable="d"
        Title="View debts" Height="300" Width="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <ListBox Grid.Column="0" Grid.Row="0" ItemsSource="{Binding Path=Debts}" Margin="5" Width="120" Height="Auto"></ListBox>
        <Button Grid.Column="0" Grid.Row="1" Height="25" Width="45" HorizontalAlignment="Left" Margin="5">Cancel</Button>
        <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="0">
            <TextBox Text="{Binding Path=Value}" Width="60" Margin="5" Height="25"></TextBox>
            <Button Command="{Binding AddDebtCommand}" Margin="5" Width="60" Height="25">Add debt</Button>
        </StackPanel>

    </Grid>
</Window>
